'use strict';

const integers: number[] = [4, 5, 6, 7];

integers.forEach(integers => console.log(integers));

/*for (let i: number = 0; i < integers.length; i++) {
    console.log(integers[i]);
} 
*/